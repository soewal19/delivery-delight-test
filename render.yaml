services:
  - type: web
    name: delivery-delight-backend
    runtime: node
    plan: free
    buildCommand: cd backend && npm install && npx prisma generate
    startCommand: cd backend && npm run start:prod
    envVars:
      - key: DATABASE_URL
        value: mongodb+srv://Admin2025:Pass321@cluster0.tavfxtx.mongodb.net/chates?retryWrites=true&w=majority&appName=Cluster
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
      - key: JWT_SECRET
        generateValue: true
      - key: JWT_EXPIRES_IN
        value: 1d
    healthCheckPath: /api/shops
    autoDeploy: true

  - type: web
    name: delivery-delight-frontend
    runtime: static
    plan: free
    buildCommand: cd frontend && npm install && npm run build
    staticPublishPath: frontend/dist
    envVars:
      - key: VITE_API_URL
        value: https://delivery-delight-backend.onrender.com/api
    autoDeploy: true
